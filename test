# Google Driveをマウントする（すでにマウントされている場合は不要）
from google.colab import drive
drive.mount('/content/drive')

# gitをインストールする（すでにインストールされている場合は不要）
!apt-get install git

# GitHubの新しいレポジトリをクローンする（ユーザ名とアクセストークンもしくはパスワードを入力する）
# レポジトリのURLはGitHubのサイトでコピーする
!git clone https://username:accesstoken@github.com/username/new_repository.git

# GitHubのユーザ名とメールアドレスを設定する
!git config --global user.name "username"
!git config --global user.email "email"

# コードを実行する（正しいパスに移動する）
%cd /content/new_repository
!python code.py

# コードを変更し、add, commit, pushする
!git add code.py
!git commit -m "message"
!git push origin main
